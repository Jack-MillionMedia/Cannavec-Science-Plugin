# Cannavec Science core is stdlib-only (Constitution §X) — the importable
# engine, curated registries, and live discovery import nothing outside the
# Python standard library. There are intentionally no runtime requirements here.
#
# NOTE: Vercel's Python builder uses `uv` and installs from pyproject.toml, NOT
# this file (see the deploy build log). The one optional dependency — the
# anthropic SDK for the LLM-rerank lift — is therefore declared in pyproject.toml
# (the `rerank` dependency group, installed by uv on the deployment). Editing
# this file does NOT change what Vercel installs. To use the rerank lift
# elsewhere: `pip install cannavec-science[rerank]`, or `uv sync`.
